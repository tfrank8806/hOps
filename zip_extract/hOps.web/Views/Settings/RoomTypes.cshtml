@model List<hOps.web.Models.RoomType>
@{
    ViewData["Title"] = "Room Types";
}

<h2>@ViewData["Title"]</h2>
<p><a class="btn btn-primary" asp-action="CreateRoomType">Add Room Type</a></p>

<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Description</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.Description</td>
                <td>
                    <a asp-action="EditRoomType" asp-route-id="@item.Id" class="btn btn-warning btn-sm">Edit</a>
                    <form asp-action="DeleteRoomType" asp-route-id="@item.Id" method="post" style="display:inline">
                        <button class="btn btn-danger btn-sm" onclick="return confirm('Delete this room type?')">Delete</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
